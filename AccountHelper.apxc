public with sharing class AccountHelper {
    @AuraEnabled(cacheable=true)
    public static List<Account> getAccountList() {
        return [SELECT Name, Owner, Phone, Website, AnnualRevenue FROM Account];
    }
}
